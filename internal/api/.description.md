### `internal/api`
Directory containing API handlers and routing.

---

### `events.go`
SSE event stream implementation stub (future feature).

#### Exports (optional)
- None (0 bytes)

### `handler.go`
HTTP request handler implementation — `/health` and `/v1/chat/completions` handlers.

#### Exports
- `Handler` struct (`LLM *llm.Client`)
- `ChatRequest` struct (`Model string`, `Messages []llm.Message`)
- `(h *Handler) HandleChatCompletion(w http.ResponseWriter, r *http.Request)`
- `(h *Handler) HandleHealth(w http.ResponseWriter, r *http.Request)`

### `middleware.go`
API middleware implementation stub (future: logging, CORS, recovery, auth).

#### Exports (optional)
- None (0 bytes)

### `routes.go`
API route registration — wires handler methods to `http.ServeMux`.

#### Exports
- `RegisterRoutes(mux *http.ServeMux, h *Handler)` — registers `/health` and `/v1/chat/completions`

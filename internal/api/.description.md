### `internal/api`
API handlers and routing — session CRUD, chat, OpenAI compatibility, EventBus.

---

### `events.go`
EventBus — concurrency-safe pub/sub for SSE events with heartbeat, session scoping, slow-consumer protection, clean shutdown via done channel.

#### Algorithm
- `EventBus` struct with `sync.RWMutex`, `global []chan SSEEvent`, `sessions map[string][]chan SSEEvent`, `done chan struct{}`
- `SSEEvent` struct: `Type string`, `Properties interface{}`
- `NewEventBus()` creates empty bus with done channel + starts heartbeat goroutine (15s ticker)
- `Subscribe(sessionID)` — returns new buffered channel (buffer=64); sessionID="" = global listener
- `Unsubscribe(sessionID, ch)` — removes channel from bus and closes it; safe to call multiple times
- `PublishGlobal(eventType, props)` / `PublishSession(sessionID, eventType, props)` — primitive-typed methods implementing `agent.EventBus` interface; wrap internal `SSEEvent` structs
- Slow listener protection: `select default` drops events when channel buffer full
- `Subscribe()` after `Close()` — returns already-closed channel (caller's `range ch` exits immediately, no goroutine leak)
- `Close()` — closes done channel (stops heartbeat), closes all listener channels, nil-ifies slices/maps, no-op if already closed
- Heartbeat goroutine uses `select` on `done` channel + ticker.C (no TOCTOU gap between closed-check and publish)

#### Exports
- `EventBus` struct (implements `agent.EventBus` interface)
- `SSEEvent` struct

### `handler.go`
HTTP request handlers with session management, chat, transactions, error handling.

#### Algorithm
**Session CRUD**: Create sessions with UUID fallback ID, list all, get by ID + messages, delete (wrapped in transaction — atomic delete messages + delete session, defer tx.Rollback, tx.Commit).  
**Chat message flow**: Read session → save user message (transaction) → update session status/active leaf → call LLM → save assistant response (transaction) → return. Uses transaction for race prevention during concurrent requests. LLM call outside transaction to avoid DB lock.  
**Stuck session fix**: `defer` in HandleChatMessage resets status to "idle" if `statusReset` flag not set (set by `saveLLMErrorAndRespond` and normal completion).  
**Error handling**: `internalError` logs + sends 500. `saveLLMErrorAndRespond` saves sanitized error message to DB (real error logged server-side, client gets "LLM request failed"), returns 502. JSON decode errors return "invalid request body" (no err.Error() leak).  
**Input size limit**: `maxContentLength = 100KB`, returns 413 if exceeded.  
**Agent model override**: Looks up agent from registry via `session.AgentName`, falls back to config default.  
**Tool_calls detection**: Checks both `finish_reason == "tool_calls"` and `len(ToolCalls) > 0` — returns error since sync handler cannot execute tools.  
**OpenAI pass-through**: `HandleChatCompletion` accepts `/v1/chat/completions`, forwards to LLM, no history persistence. Returns `Allow: POST` header on 405. LLM error returns sanitized "llm error" (not err.Error() leak).  
**`dbMessagesToLLM`**: Converts DB messages to LLM format — preserves user, assistant (with `ToolCalls` from `ToolArgs` JSON), and tool messages (with `ToolCallID`, `Name`). Uses switch on m.Role.  
**UUID generation**: `newID()` uses `crypto/rand` hex string, fallback to timestamp-based on failure.

#### Key Functions
- `HandleCreateSession`: Decode request → default agent/title → insert session → return 201  
- `HandleListSessions`: List all sessions → convert to views → JSON encode  
- `HandleGetSession`: Get by ID + messages → return view with messages  
- `HandleDeleteSession`: BeginTx → qtx.DeleteMessages → qtx.DeleteSession → Commit → return status  
- `HandleChatMessage`: Transaction (read + save user + update status) → LLM call (check choices empty + tool_calls) → saveAssistantMessage → return response. Defer resets status on failure.  
- `saveAssistantMessage`: Inserts assistant msg (no `Parts` field), updates session status+active leaf in tx  
- `saveLLMErrorAndRespond`: Logs real error server-side, saves sanitized "LLM request failed" to DB, returns 502 with sanitized error  
- `HandleHealth`: Simple JSON health check  
- `HandleChatCompletion`: Decode OpenAI request → forward to LLM → return raw response. `Allow: POST` on 405. Sanitized "llm error" on failure.  

#### Data
- **Structures**:  
  - `Handler { LLM *llm.Client, DB *db.Queries, SQLDB *sql.DB, Config *config.Config, Engine *agent.Engine, EventBus *EventBus }`  
  - `ChatRequest { Model, Messages []llm.Message, Tools []llm.ToolDefinition, ToolChoice json.RawMessage }`  
  - `CreateSessionRequest { Title, AgentName }`  
  - `ChatResponse { SessionID, MessageID, Role, Content, Model, Usage *llm.Usage, Error }`  
  - `SessionView { ID, AgentName, Title, Status, MessageCount, CreatedAt, UpdatedAt, Messages []MessageView }`  
  - `MessageView { ID, Role, Content, AgentName, CreatedAt }`  
- **Types**: UUID string (hex format), timestamps in UnixMilli  
- **Flow**: POST `/session` → create → return view. GET `/session/{id}` → load session + messages → return view. POST `/session/{id}/message` → transaction → LLM (model from agent lookup) → check tool_calls → transaction → return response. POST `/v1/chat/completions` → forward to LLM → return raw.

#### External Dependencies
- `./internal/config`
- `./internal/db`
- `./internal/llm`

### `middleware.go`
API middleware stub — package declaration only.

#### Exports
- None (1 byte — no implementation)

### `routes.go`
Register routes on `http.ServeMux` using Go 1.22+ method routing patterns.

#### Algorithm
Wires all handler methods to serve mux with explicit method + path patterns. No middleware yet.

#### Key Functions
- `RegisterRoutes(mux *http.ServeMux, h *Handler)`:  
  - `GET /health` → `HandleHealth`  
  - `POST /session` → `HandleCreateSession`  
  - `GET /session` → `HandleListSessions`  
  - `GET /session/{id}` → `HandleGetSession`  
  - `DELETE /session/{id}` → `HandleDeleteSession`  
  - `POST /session/{id}/message` → `HandleChatMessage`  
  - `POST /v1/chat/completions` → `HandleChatCompletion`

#### Data
- **Flow**: Serve mux → register pattern → handler method
